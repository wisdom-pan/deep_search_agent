# 知识图谱初始化服务 Dockerfile
FROM swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/python:3.10-slim

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    curl \
    wget \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt -i https://mirrors.cloud.tencent.com/pypi/simple

# 复制应用代码
COPY . .

# 确保files目录存在
RUN mkdir -p files

# 暴露端口（如果有需要）
EXPOSE 8000

# 启动命令由docker-compose.yaml提供
CMD ["python", "-m", "graphrag_agent.integrations.build.main"]
